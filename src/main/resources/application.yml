debug: true
server:
  port:
spring:
  sql:
    init:
      platform: postgresql
      mode: always
  output:
    ansi:
      enabled: always
  datasource:
    driver-class-name:
    url: jdbc:postgresql://snuffleupagus.db.elephantsql.com/xqppfgop
    username: xqppfgop
    password: KXZjYAhpTN-rf_0JcRR6TpOaTsSASDT0

  jpa:
    show-sql: true
    generate-ddl: true
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: create-drop
logging:
  level:
    org:
      springframework:
        boot:
          autoconfigure: ERROR